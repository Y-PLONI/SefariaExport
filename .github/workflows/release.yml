name: Sefaria Export (Self-hosted, Python 3.9, export_all_merged → branch root)

on:
  workflow_dispatch:
    inputs:
      timezone:
        description: "Timezone for timestamp/tag (IANA name)"
        required: false
        default: "Asia/Jerusalem"

permissions:
  contents: write

concurrency:
  group: sefaria-export
  cancel-in-progress: false

env:
  TZ_NAME: ${{ inputs.timezone || 'Asia/Jerusalem' }}
  DJANGO_SETTINGS_MODULE: sefaria.settings
  MONGO_HOST: 127.0.0.1
  MONGO_PORT: 27017
  MONGO_DB_NAME: sefaria
  PYTHONDONTWRITEBYTECODE: 1

jobs:
  prep:
    name: Compute timestamp
    runs-on: [self-hosted, Linux, X64]
    outputs:
      stamp: ${{ steps.ts.outputs.stamp }}
    steps:
      - name: Compute release timestamp
        id: ts
        run: |
          TZ="${TZ_NAME}" date '+%Y-%m-%d_%H-%M' > ts.txt
          echo "stamp=$(cat ts.txt)" >> $GITHUB_OUTPUT
          echo "⏱  Using TZ=${TZ_NAME} -> $(cat ts.txt)"

  export_and_push:
    name: Export export_all_merged and push to date branch
    runs-on: [self-hosted, Linux, X64]
    needs: prep
    env:
      TS_STAMP: ${{ needs.prep.outputs.stamp }}
      SEFARIA_EXPORT_BASE: ${{ github.workspace }}/staging
      SEFARIA_EXPORT_PATH: ${{ github.workspace }}/staging
      VENV: ${{ github.workspace }}/.venv
    steps:
      - name: Checkout .github (sparse only)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main
          sparse-checkout: |
            .github
          sparse-checkout-cone-mode: false

      - name: Ensure base tools (apt/dnf/yum/apk autodetect)
        shell: bash
        run: |
          set -e
          if command -v apt-get >/dev/null 2>&1; then
            sudo -n apt-get update -y
            sudo -n apt-get install -y ca-certificates tar wget rsync coreutils netcat-openbsd curl git
            sudo -n apt-get install -y aria2 || true
            sudo -n apt-get install -y build-essential cmake ninja-build pybind11-dev libre2-dev || true
          elif command -v dnf >/dev/null 2>&1; then
            sudo -n dnf install -y ca-certificates tar wget rsync coreutils nmap-ncat curl git
            sudo -n dnf install -y aria2 || true
            sudo -n dnf install -y gcc gcc-c++ make cmake ninja-build pybind11-devel re2-devel || true
          elif command -v yum >/dev/null 2>&1; then
            sudo -n yum install -y ca-certificates tar wget rsync coreutils nmap-ncat curl git
            sudo -n yum install -y aria2 || true
            sudo -n yum install -y gcc gcc-c++ make cmake ninja-build pybind11-devel re2-devel || true
          elif command -v apk >/dev/null 2>&1; then
            sudo -n apk add --no-cache ca-certificates tar wget rsync coreutils netcat-openbsd curl git aria2 build-base cmake ninja py3-pybind11 re2-dev
          fi
          echo "✅ System tools ensured."

      - name: Set up Python 3.9 (actions/setup-python) + create venv
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      - name: Create and activate venv (PEP 668 safe)
        run: |
          python -V
          python -m venv "$VENV"
          . "$VENV/bin/activate"
          python -V
          pip -V
          pip install --upgrade pip setuptools wheel

      - name: Start MongoDB (prefer Docker with sudo, else expect local mongod)
        id: mongostart
        shell: bash
        run: |
          set -e
          if command -v docker >/dev/null 2>&1; then
            echo "Using Dockerized MongoDB..."
            sudo -n docker rm -f gha-mongo >/dev/null 2>&1 || true
            sudo -n docker run -d --name gha-mongo -p 27017:27017 mongo:6.0
            echo "docker=1" >> $GITHUB_OUTPUT
          else
            echo "⚠️ Docker not found. Expecting local mongod on 127.0.0.1:27017"
            echo "docker=0" >> $GITHUB_OUTPUT
          fi

      - name: Wait for MongoDB TCP (safety)
        run: |
          for i in {1..120}; do
            if (echo > /dev/tcp/127.0.0.1/27017) >/dev/null 2>&1; then
              echo "✅ MongoDB TCP port is reachable"; exit 0
            fi
            echo "⏳ Waiting for MongoDB TCP..."; sleep 1
          done
          echo "❌ MongoDB did not become reachable"; exit 1

      - name: Ensure mongorestore (Database Tools)
        shell: bash
        run: |
          if ! command -v mongorestore >/dev/null 2>&1; then
            echo "Installing MongoDB Database Tools..."
            TOOLS_VER="100.9.4"
            if command -v apt-get >/dev/null 2>&1; then
              wget -q https://fastdl.mongodb.org/tools/db/mongodb-database-tools-ubuntu2204-x86_64-${TOOLS_VER}.tgz
              tar -xzf mongodb-database-tools-ubuntu2204-x86_64-${TOOLS_VER}.tgz
              sudo -n mv mongodb-database-tools-ubuntu2204-x86_64-${TOOLS_VER}/bin/* /usr/local/bin/
              rm -rf mongodb-database-tools-ubuntu2204-x86_64-${TOOLS_VER}*
            else
              echo "⚠️ Skipping tools install (non-Debian). Assume mongorestore exists or provide manually."
            fi
          fi
          mongorestore --version || true

      - name: Clone Sefaria-Project
        run: |
          git clone --depth 1 https://github.com/Sefaria/Sefaria-Project.git
          ls -la Sefaria-Project

      - name: Prepare constraints (skip google-re2, force psycopg2-binary)
        working-directory: Sefaria-Project
        run: |
          # Build a filtered requirements file without google-re2 & psycopg2
          awk 'BEGIN{IGNORECASE=1} !/^\s*(google-re2|psycopg2)\b/' requirements.txt > req.filtered.txt
          printf "\n# Overrides for CI:\npsycopg2-binary\n" >> req.filtered.txt
          cat req.filtered.txt

      - name: Install Python deps into venv (Cython first, then reqs, then built-google-re2)
        working-directory: Sefaria-Project
        run: |
          set -e
          . "$VENV/bin/activate"
          python -m pip install --upgrade pip setuptools wheel
          # Some packages expect Cython present during metadata/build
          pip install "Cython<3.0" || pip install Cython
          # Install filtered requirements (no google-re2, no psycopg2 source)
          pip install --no-cache-dir -r req.filtered.txt
          # Install built-google-re2 wheel (avoids Bazel)
          pip install --no-cache-dir built-google-re2
          python - <<'PY'
          import re, pkgutil, sys
          assert pkgutil.find_loader("re2"), "built-google-re2 not importable"
          print("✅ built-google-re2 available as re2")
          PY

      - name: Create minimal local_settings.py (SEFARIA_DB + export path)
        working-directory: Sefaria-Project/sefaria
        run: |
          . "$VENV/bin/activate"
          mkdir -p "$SEFARIA_EXPORT_PATH"
          cp local_settings_example.py local_settings.py
          python - <<'PY'
          import os, re
          p = "local_settings.py"
          with open(p, "r", encoding="utf-8") as f:
              s = f.read()
          s = re.sub(r"SEFARIA_EXPORT_PATH\s*=.*", f'SEFARIA_EXPORT_PATH = r"{os.environ["SEFARIA_EXPORT_PATH"]}"', s)
          s = re.sub(r"MONGO_HOST\s*=.*", f'MONGO_HOST = "{os.environ["MONGO_HOST"]}"', s)
          s = re.sub(r"MONGO_PORT\s*=.*", f'MONGO_PORT = {int(os.environ["MONGO_PORT"])}', s)
          s = re.sub(r"MONGO_DB_NAME\s*=.*", f'MONGO_DB_NAME = "{os.environ["MONGO_DB_NAME"]}"', s)
          if re.search(r"SEFARIA_DB\s*=", s):
              s = re.sub(r"SEFARIA_DB\s*=.*", 'SEFARIA_DB = "sefaria"', s)
          else:
              s += '\nSEFARIA_DB = "sefaria"\n'
          with open(p, "w", encoding="utf-8") as f:
              f.write(s)
          print("✅ local_settings.py configured (SEFARIA_DB=sefaria)")
          PY

      - name: Download small Mongo dump
        run: |
          mkdir -p mongo_dump
          if command -v aria2c >/dev/null 2>&1; then
            aria2c -x 16 -s 16 -k 1M -o dump_small.tar.gz "https://storage.googleapis.com/sefaria-mongo-backup/dump_small.tar.gz"
          else
            wget -O dump_small.tar.gz "https://storage.googleapis.com/sefaria-mongo-backup/dump_small.tar.gz"
          fi
          tar -xzf dump_small.tar.gz -C mongo_dump
          rm -f dump_small.tar.gz || true
          echo "Dump tree:"; find mongo_dump -maxdepth 3 | sed -e 's/^/  /'

      - name: Normalize layout to mongo_dump_pkg/sefaria
        run: |
          mkdir -p mongo_dump_pkg
          if [ -d mongo_dump/dump/sefaria ]; then
            cp -a mongo_dump/dump/sefaria mongo_dump_pkg/
          elif [ -d mongo_dump/sefaria ]; then
            cp -a mongo_dump/sefaria mongo_dump_pkg/
          else
            echo "❌ Could not find 'sefaria' folder in dump"; exit 1
          fi
          echo "Packaged tree:"; find mongo_dump_pkg -maxdepth 2 | sed -e 's/^/  /'

      - name: Restore DB and run export_all_merged
        env:
          PYTHONPATH: ${{ github.workspace }}/Sefaria-Project
        run: |
          set -euo pipefail
          . "$VENV/bin/activate"

          # Restore DB
          mongorestore --host 127.0.0.1 --port 27017 --drop --db sefaria "mongo_dump_pkg/sefaria"

          python - <<'PY'
          from pymongo import MongoClient, errors
          client = MongoClient("mongodb://127.0.0.1:27017")
          db = client["sefaria"]
          try:
              db.create_collection("history")
              print("Created empty 'history' collection.")
          except errors.CollectionInvalid:
              print("'history' collection already exists.")
          PY

          # Point SEFARIA_EXPORT_PATH to staging/export_all_merged
          export CUR_EXPORT="export_all_merged"
          DEST_DIR="$SEFARIA_EXPORT_BASE/$CUR_EXPORT"
          mkdir -p "$DEST_DIR"
          python - <<'PY'
          import os, re
          p = os.path.join("Sefaria-Project","sefaria","local_settings.py")
          with open(p, "r", encoding="utf-8") as f:
              s = f.read()
          dest = os.path.join(os.environ["GITHUB_WORKSPACE"], "staging", "export_all_merged")
          s = re.sub(r"SEFARIA_EXPORT_PATH\s*=.*", f'SEFARIA_EXPORT_PATH = r"{dest}"', s)
          with open(p, "w", encoding="utf-8") as f:
              f.write(s)
          print("➡️  SEFARIA_EXPORT_PATH set to:", dest)
          PY

          # Run export_all_merged
          python - <<'PY'
          import os, sys, django
          sys.path.insert(0, os.path.abspath("Sefaria-Project"))
          os.environ.setdefault("DJANGO_SETTINGS_MODULE", "sefaria.settings")
          django.setup()
          from sefaria import export as ex
          ex.export_all_merged()
          print("✅ export_all_merged completed successfully.")
          PY

          # Gather outputs to FINAL_DIR
          FINAL_DIR="$RUNNER_TEMP/final_root"
          mkdir -p "$FINAL_DIR"
          rsync -a "$DEST_DIR/" "$FINAL_DIR/" || cp -a "$DEST_DIR/." "$FINAL_DIR/"

      - name: Drop DB and cleanup
        if: always()
        run: |
          set +e
          echo "🗑️ Dropping MongoDB database 'sefaria'..."
          mongosh --quiet --eval 'db.getSiblingDB("sefaria").dropDatabase(); print("✅ Database dropped.")' 2>/dev/null || true
          sudo -n docker rm -f gha-mongo >/dev/null 2>&1 || true
          rm -rf mongo_dump mongo_dump_pkg || true
          echo "Disk usage after cleanup:"; df -h || true

      - name: Push exported content to date-named branch (root + keep .github)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          REPO_DIR="$RUNNER_TEMP/pushrepo"
          rm -rf "$REPO_DIR" && mkdir -p "$REPO_DIR"
          git -C "$REPO_DIR" init
          git -C "$REPO_DIR" config user.name "github-actions[bot]"
          git -C "$REPO_DIR" config user.email "github-actions[bot]@users.noreply.github.com"
          git -C "$REPO_DIR" remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
          TS_BRANCH="${TS_STAMP}"
          git -C "$REPO_DIR" checkout --orphan "$TS_BRANCH"
          # Keep CI
          mkdir -p "$REPO_DIR/.github"
          cp -a "$GITHUB_WORKSPACE/.github/." "$REPO_DIR/.github/"
          # Copy export to root
          FINAL_DIR="$RUNNER_TEMP/final_root"
          cp -a "$FINAL_DIR/." "$REPO_DIR/"
          cd "$REPO_DIR"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to push; skipping commit."
          else
            git commit -m "chore(export): export_all_merged at ${TS_STAMP} [skip ci]"
            git push --force origin HEAD:"$TS_BRANCH"
            echo "✅ Pushed to branch ${TS_BRANCH}"
          fi
